using System;
using System.Collections.Generic;
using System.Text;

namespace SmartKart.Identity.Persistence.Constants
{
    internal static class TableNames
    {
        internal const string tbl_Users = nameof(tbl_Users);

        internal const string tbl_Roles = nameof(tbl_Roles);
    }
}
